[project]
name = "luna"
version = "0.1.0"
description = "Your coder — an opencode-style CLI coding assistant"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]